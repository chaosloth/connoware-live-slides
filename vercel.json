{
  "version": 2,
  "buildCommand": "npm run vercel-build",
  "outputDirectory": ".next",
  "installCommand": "npm install --legacy-peer-deps && npm install source-map source-map-support",
  "framework": "nextjs",
  "env": {
    "NEXT_WEBPACK": "true",
    "NODE_OPTIONS": "--max-old-space-size=4096"
  }
}
